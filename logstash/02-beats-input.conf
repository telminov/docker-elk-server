input {
   stdin {
       codec => "json"
   }
   beats {
       codec => "json"
       port => 5044
       ssl => true
       ssl_certificate => "/tls/certs/logstash-forwarder.crt"
       ssl_key => "/tls/private/logstash-forwarder.key"
   }
}
